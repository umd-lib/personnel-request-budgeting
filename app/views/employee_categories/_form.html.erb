<%= form_for(@employee_category) do |f| %>
  <% if @employee_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_category.errors.count, "error") %> prohibited this employee_category from being saved:</h2>

      <ul>
      <% @employee_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-striped">
    <tbody>
      <tr>
        <th><%= f.label :code %></th>
        <td><%= f.text_field :code %></td>
      </tr>
      <tr>
        <th><%= f.label :name %></th>
        <td><%= f.text_field :name %></td>
      </tr>
    </tbody>
  </table>

  <%= render partial: 'shared/form_action_buttons', locals: { form: f, object: @employee_category } %>
<% end %>
