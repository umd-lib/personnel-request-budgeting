<% klass = archive? ? ArchivedLaborRequest : LaborRequest %>
<div class="page-header">
  <h1>Labor and Assistance Requests</h1>
</div>

<%= render partial: 'shared/index_action_buttons',
  locals: { klass: klass, pagination_object: @labor_requests } %>
<%= render partial: 'shared/table', locals: { klass: klass,  records: @labor_requests } %>
<%= will_paginate renderer: BootstrapPagination::Rails %>
