<%= javascript_tag do %>
  var treeData = <%=  @organizations.to_json.html_safe %>;
  $(function() {
      var tree = new OrganizationTree(treeData);
      <% if params["action"] == "index" %> 
        tree.index();
      <% else %>
        tree.show();
    <% end %>
  });
<% end %>
