<%= form_for(@role_cutoff) do |f| %>
  <% if @role_cutoff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_cutoff.errors.count, "error") %> prohibited this role_cutoff from being saved:</h2>

      <ul>
      <% @role_cutoff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role_type_id %><br>
    <%= f.text_field :role_type_id %>
  </div>
  <div class="field">
    <%= f.label :cutoff_date %><br>
    <%= f.date_select :cutoff_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
