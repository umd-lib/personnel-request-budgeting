<div class="container">
<div class="page-header">
  <h1><%= @role_cutoff.role_type.name %> <small>Role Cutoff</small></h1>
</div>

<table class="table table-striped">
  <tbody>
    <tr>
      <th>Role type:</th>
      <td id="role_type"><%= @role_cutoff.role_type.name %></td>
    </tr>
    
    <tr>
      <th>Name:</th>
      <td id="cutoff_date"><%= @role_cutoff.cutoff_date %></td>
    </tr>
  </tbody>
</table>

<%= render 'shared/show_action_buttons', object: @role_cutoff %>
</div>
