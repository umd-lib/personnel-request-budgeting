<%#
# Displays validation errors that occurred when submitting a form
%>

<% if errors.any? %>
  <div class="alert alert-danger">
    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
