<%= form_for(@subdepartment) do |f| %>
  <% if @subdepartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdepartment.errors.count, "error") %> prohibited this subdepartment from being saved:</h2>

      <ul>
      <% @subdepartment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :department_id %><br>
    <%= select("subdepartment", "department_id", Department.all.collect {|d| [ d.name, d.id ] }, {prompt: 'Select Department'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
