<%= form_for(@subdepartment) do |f| %>
  <% if @subdepartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdepartment.errors.count, "error") %> prohibited this subdepartment from being saved:</h2>

      <ul>
      <% @subdepartment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-striped">
    <tbody>
      <tr>
        <th><%= f.label :code %></th>
        <td><%= f.text_field :code %></td>
      </tr>
      <tr>
        <th><%= f.label :name %></th>
        <td><%= f.text_field :name %></td>
      </tr>
      <tr>
        <th><%= f.label :department_id %></th>
        <td><%= select("subdepartment", "department_id", Department.all.collect {|d| [ d.name, d.id ] }, {prompt: 'Select Department'}) %></td>
      </tr>
    </tbody>
  </table>
    
  <%= render partial: 'shared/form_action_buttons', locals: { form: f, object: @subdepartment } %>
<% end %>
